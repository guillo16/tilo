<div class="line-wrapper">
  <div class="line-image">
    <% line_item.product.photos.first(1).each do |photo| %>
    <%= cl_image_tag photo.key%>
    <% end %>
  </div>
  <div class="line-content">
    <h2><%= line_item.product.title %></h2>
    <% if line_item.product.stock > 1 %>
    <%= link_to  edit_line_item_path(line_item) do %>
    <div class="line-quantity">
      <%= line_item.quantity %>
      <i class="fas fa-angle-down"></i>
    </div>
    <% end %>
    <% else %>
    <div class="line-quantity">
      <%= line_item.quantity %>
      (ultima unidad)
    </div>
    <% end %>
    <div class="line-price">
      <p><strong>PRECIO:</strong> <%= number_to_currency(line_item.total_price) %></p>
    </div>
    <div class="line-description">
      <%= link_to 'Quitar Articulo', line_item, method: :delete, data: { confirm: "Are you sure? " }  %>
    </div>
  </div>
</div>

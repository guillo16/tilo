<div class="p-5 text-center home-title">
  <h2>Novedades</h2>
  <h3></h3>
</div>
<div class="scrolling-wrapper">
  <div class="home-card-container">
    <% @products.last(8).uniq(&:title).reverse_each do |product| %>
    <%= render 'shared/home_cards', product: product %>
    <% end %>
  </div>
</div>
<div class="p-5 text-center home-title">
  <h2>Destacados</h2>
  <h3></h3>
</div>
<div class="scrolling-wrapper">
  <div class="home-card-container">
    <% @products.last(8).uniq(&:title).each do |product| %>
    <%= render 'shared/home_cards', product: product %>
    <% end %>
  </div>
</div>
